{
  "name": "site-books",
  "scripts": {
    "preparcel": "rm -rf dist",
    "parcel": "parcel $RUN src/index.html",
    "build": "RUN=build npm run parcel",
    "start": "RUN=serve npm run parcel",
    "lint": "eslint . --fix"
  },
  "devDependencies": {
    "@parcel/packager-raw-url": "^2.0.0-beta.3.1",
    "@parcel/transformer-webmanifest": "^2.0.0-beta.3.1",
    "eslint": "^7.27.0",
    "eslint-plugin-node": "^11.1.0",
    "parcel": "^2.0.0-beta.3.1"
  }
}
